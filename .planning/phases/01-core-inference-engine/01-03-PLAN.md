---
phase: 01-core-inference-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - crates/hephaestus/Cargo.toml
  - crates/hephaestus/src/config.rs
  - crates/hephaestus/src/main.rs
autonomous: true
requirements:
  - CORE-02
  - CORE-03
  - XCUT-03

must_haves:
  truths:
    - "Binary loads configuration from environment variables via envy crate"
    - "MODEL_ID is required -- binary crashes with clear error if missing"
    - "Binary constructs ClassifierPipeline from MODEL_PATH directory"
    - "Binary runs warmup inference pass and logs the result before reporting ready"
    - "Optional env vars (MODEL_PATH, EXECUTION_PROVIDER, LOG_LEVEL, WARMUP_INPUT) use sensible defaults"
  artifacts:
    - path: "crates/hephaestus/src/config.rs"
      provides: "Config struct with envy deserialization for MODEL_ID, MODEL_PATH, EXECUTION_PROVIDER, LOG_LEVEL, WARMUP_INPUT"
    - path: "crates/hephaestus/src/main.rs"
      provides: "Binary entry point: config load -> pipeline construction -> warmup -> ready"
  key_links:
    - from: "crates/hephaestus/src/main.rs"
      to: "crates/hephaestus/src/config.rs"
      via: "main() calls Config::from_env() to load typed configuration"
    - from: "crates/hephaestus/src/main.rs"
      to: "crates/hephaestus-core/src/pipeline.rs"
      via: "main() calls ClassifierPipeline::new(model_dir) then pipeline.prepare() + pipeline.execute() for warmup"
---

## Phase Goal

**As a** developer, **I want to** load an ONNX classifier model and run text classification inference programmatically, **so that** I can validate the core inference pipeline works end-to-end before adding HTTP serving.

<objective>
Wire the binary entry point: load configuration from environment variables, construct the ClassifierPipeline, run a warmup inference pass, and report ready. After this plan, a developer can run the hephaestus binary with MODEL_ID and MODEL_PATH env vars and see it load a real model, classify warmup text, and report ready.

Implements D-11 (envy config, no Clap), D-12 (env var definitions), D-13 (fail hard on required config), CORE-02 (config from env vars), CORE-03 (warmup pass), XCUT-03 (rules compliance).

Purpose: Complete the walking skeleton -- a runnable binary that exercises the full inference pipeline.
Output: Working binary that loads model, validates, warms up, and reports ready.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-inference-engine/01-CONTEXT.md
@.planning/phases/01-core-inference-engine/01-RESEARCH.md
@.planning/phases/01-core-inference-engine/01-01-SUMMARY.md
@.planning/phases/01-core-inference-engine/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Config struct and main.rs startup sequence</name>
  <files>crates/hephaestus/Cargo.toml, crates/hephaestus/src/config.rs, crates/hephaestus/src/main.rs</files>
  <read_first>
    - crates/hephaestus/Cargo.toml (current dependencies from Plan 01)
    - crates/hephaestus/src/main.rs (current placeholder from Plan 01)
    - crates/hephaestus-core/src/pipeline.rs (ClassifierPipeline::new signature and Pipeline trait)
    - crates/hephaestus-core/src/lib.rs (public re-exports available)
    - .planning/phases/01-core-inference-engine/01-RESEARCH.md (Pattern 5: Config with envy, warmup pattern)
    - .planning/phases/01-core-inference-engine/01-CONTEXT.md (D-11, D-12, D-13)
    - rules/err-anyhow-app.md (anyhow usage in application binary)
  </read_first>
  <action>
    Implement typed configuration and binary startup per D-11, D-12, D-13.

    Cargo.toml: Verify dependencies include hephaestus-core (path), anyhow.workspace, envy.workspace, serde.workspace, tracing.workspace. Add tracing-subscriber = { version = "0.3", features = ["json", "env-filter"] } to workspace.dependencies in root Cargo.toml first, then add tracing-subscriber.workspace = true to this crate. tracing-subscriber is needed for structured log output in main() -- sets up the fmt subscriber with env filter.

    config.rs: Create Config struct with serde Deserialize derive. Fields per D-12:
    - model_id: String (required -- envy will fail deserialization if MODEL_ID env var is missing, which is the desired crash behavior per D-13)
    - model_path: Option of String with #[serde(default)] (optional -- local directory override)
    - execution_provider: String with #[serde(default = "default_ep")] where default_ep returns "cpu"
    - log_level: String with #[serde(default = "default_log_level")] where default_log_level returns "info"
    - warmup_input: Option of String with #[serde(default)] (optional custom warmup text)

    Add a pub fn from_env() -> Result of Config or anyhow::Error method that wraps envy::from_env::<Config>() with anyhow context mentioning MODEL_ID is required.

    Add a pub fn model_dir(&self) -> std::path::PathBuf method that returns PathBuf from self.model_path if Some, otherwise panics with a message that MODEL_PATH is required for Phase 1 (Phase 3 adds automatic model resolution). For T-01-01 mitigation: validate that the path is absolute (starts with /) and does not contain ".." components. Return an error if validation fails.

    Actually, make model_dir() return Result of PathBuf or anyhow::Error to handle the validation. Check: path exists, path is absolute, path does not contain ".." segments (iterate path.components() and reject Component::ParentDir). If MODEL_PATH is None, return error "MODEL_PATH is required (model resolution not yet implemented -- Phase 3)".

    main.rs: Replace placeholder with real startup sequence. fn main() -> Result of () or anyhow::Error (per rule err-anyhow-app: use anyhow in application binary).

    Startup sequence:
    1. Initialize tracing subscriber: tracing_subscriber::fmt().with_env_filter(tracing_subscriber::EnvFilter::from_default_env()).json().init() -- structured JSON logs. If LOG_LEVEL env is set, it is picked up by EnvFilter.
    2. Load config: let config = Config::from_env()?; Log config summary (model_id, execution_provider) at info level. Do NOT log model_path (may contain sensitive directory structure).
    3. Resolve model directory: let model_dir = config.model_dir()?; -- validates path per T-01-01.
    4. Construct pipeline: let mut pipeline = hephaestus_core::ClassifierPipeline::new(&model_dir)?; Log success at info level.
    5. Run warmup: let warmup_text = config.warmup_input.as_deref().unwrap_or("This is a warmup inference pass."); let prepared = pipeline.prepare(warmup_text.to_string())?; let output = pipeline.execute(prepared)?; Log warmup result (label, score) at info level.
    6. Log "hephaestus ready" at info level. Phase 2 will add HTTP server start here.
    7. Return Ok(())

    Per rule err-no-unwrap-prod: use ? throughout main. The only unwrap_or is for the warmup text default, which is safe.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith && cargo build -p hephaestus 2>&1 | tail -5 && cargo clippy -p hephaestus -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - crates/hephaestus/src/config.rs defines pub struct Config with fields model_id (String), model_path (Option), execution_provider (String with default "cpu"), log_level (String with default "info"), warmup_input (Option)
    - Config has pub fn from_env() returning Result
    - Config has pub fn model_dir() returning Result with path validation (absolute, no ".." components)
    - main.rs initializes tracing subscriber with JSON format
    - main.rs loads Config via from_env(), constructs ClassifierPipeline, runs warmup inference
    - main.rs uses anyhow::Error as return type (per err-anyhow-app rule)
    - No unwrap() calls in production code paths
    - cargo build -p hephaestus exits 0
    - cargo clippy -p hephaestus -- -D warnings exits 0
  </acceptance_criteria>
  <done>Config struct loads from env vars via envy. main.rs startup: config -> path validation -> pipeline construction -> warmup -> ready. Binary compiles and passes clippy.</done>
</task>

<task type="auto">
  <name>Task 2: Add config unit tests and run full workspace verification</name>
  <files>crates/hephaestus/src/config.rs</files>
  <read_first>
    - crates/hephaestus/src/config.rs (Config implementation from Task 1)
    - rules/test-cfg-test-module.md (test module pattern)
    - rules/test-arrange-act-assert.md (test structure)
  </read_first>
  <action>
    Add unit tests for Config and run the full verification suite.

    In config.rs, add #[cfg(test)] mod tests with:
    - test_from_env_missing_model_id: temporarily clear MODEL_ID env var, call from_env(), assert it returns Err. Use std::env::remove_var in test setup. Note: env var tests may need to run serially if they modify process-wide env state -- consider using temp_env crate pattern (set and restore) or accept serial test execution.
    - test_from_env_with_defaults: set MODEL_ID="test-model" env var, call from_env(), assert execution_provider is "cpu" and log_level is "info"
    - test_model_dir_rejects_relative_path: create Config with model_path = Some("relative/path"), call model_dir(), assert Err
    - test_model_dir_rejects_parent_traversal: create Config with model_path containing ".." component, call model_dir(), assert Err
    - test_model_dir_accepts_valid_absolute_path: create Config with model_path = Some(tempdir path), call model_dir(), assert Ok with correct PathBuf

    For tests that need a Config with specific field values without going through envy, add a helper constructor (e.g., Config::new_for_test or just construct the struct directly if fields are pub).

    After adding tests, run the full verification suite:
    1. cargo test --workspace (all unit + lib tests)
    2. cargo clippy --workspace -- -D warnings (lint check)
    3. cargo test -p hephaestus-core --test classifier_e2e -- --ignored (integration test with real model -- confirms GREEN state still holds)
    4. cargo build --workspace (full build)

    This verifies the complete Phase 1 walking skeleton works end-to-end.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith && cargo test --workspace 2>&1 | tail -15 && cargo clippy --workspace -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - config.rs contains #[cfg(test)] mod tests with at least 4 test functions
    - test_model_dir_rejects_relative_path passes (relative paths rejected)
    - test_model_dir_rejects_parent_traversal passes (.. components rejected)
    - cargo test --workspace exits 0 (all unit tests across all crates pass)
    - cargo clippy --workspace -- -D warnings exits 0 (no warnings in any crate)
    - cargo build --workspace exits 0
  </acceptance_criteria>
  <done>Config unit tests pass. Full workspace builds, all tests pass, no clippy warnings. Walking skeleton complete: binary loads config from env, constructs pipeline, runs warmup, reports ready.</done>
</task>

</tasks>

## Artifacts This Phase Produces

### Plan 01-03 Symbols

- `Config` struct in `hephaestus::config` -- { model_id, model_path, execution_provider, log_level, warmup_input }
- `Config::from_env()` method -- wraps envy::from_env with anyhow context
- `Config::model_dir()` method -- validates and returns model directory path
- `fn main()` in `hephaestus` -- startup sequence: tracing init -> config -> pipeline -> warmup -> ready
- `tracing-subscriber` added to `[workspace.dependencies]` in root Cargo.toml
- File: `crates/hephaestus/src/config.rs` (new)
- Modified: `crates/hephaestus/src/main.rs` (replaced placeholder), `crates/hephaestus/Cargo.toml` (added deps)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ENV -> Config | Environment variables are untrusted input parsed into typed Config struct via envy |
| Config -> Filesystem | MODEL_PATH from env determines which directory is loaded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Config::model_dir() path validation | medium | mitigate | Config::model_dir() validates MODEL_PATH is absolute (starts with /) and contains no Component::ParentDir segments. Rejects relative paths and traversal attempts with descriptive error. |
| T-01-02 | Denial of Service | Pipeline::prepare() tokenization | medium | mitigate | Already mitigated in Plan 02 -- tokenizer truncation to max_length 512. |
| T-01-03 | Denial of Service | Model loading at startup | low | accept | One-time startup cost. K8s pod resource limits apply. |
| T-01-04 | Information Disclosure | CoreError/anyhow error Display | low | accept | Internal service. main.rs logs errors via tracing (structured JSON). No external exposure in Phase 1. Phase 2 HTTP layer sanitizes responses. |
| T-01-SC | Tampering | cargo dependency installs | high | mitigate | tracing-subscriber is a widely-used crate (10M+ weekly downloads, maintained by tokio-rs). All other packages already audited in Plan 01. |
</threat_model>

<verification>
- `cargo build --workspace` -- full workspace compiles
- `cargo test --workspace` -- all unit tests pass
- `cargo clippy --workspace -- -D warnings` -- no warnings
- `MODEL_ID=test MODEL_PATH=/nonexistent cargo run -p hephaestus 2>&1` -- binary starts, attempts model load, fails with descriptive error about missing model files (not a crash from missing config)
- `cargo run -p hephaestus 2>&1` -- without MODEL_ID, binary crashes with clear error mentioning MODEL_ID
</verification>

<success_criteria>
- Config loads from env vars: MODEL_ID required, others optional with defaults (CORE-02)
- Missing MODEL_ID causes clear crash message (D-13)
- MODEL_PATH validated: absolute, no traversal (T-01-01)
- main.rs constructs pipeline and runs warmup inference pass (CORE-03)
- Warmup logs label and score at info level
- Full workspace passes build, test, and clippy (XCUT-03)
- Walking skeleton complete: developer sets MODEL_ID + MODEL_PATH, binary loads model and reports ready
</success_criteria>

<output>
Create `.planning/phases/01-core-inference-engine/01-03-SUMMARY.md` when done
</output>

---
phase: 02-http-serving-and-observability
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/hephaestus-api/Cargo.toml
  - crates/hephaestus-api/src/lib.rs
  - crates/hephaestus-api/tests/api.rs
  - crates/hephaestus-api/tests/health.rs
  - crates/hephaestus-api/tests/shutdown.rs
  - crates/hephaestus-api/tests/metrics.rs
  - crates/hephaestus-api/tests/tracing.rs
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - CORE-04
  - OBSV-01
  - OBSV-02
  - OBSV-03

must_haves:
  truths:
    - "Integration test files exist for all Phase 2 requirements: api.rs, health.rs, shutdown.rs, metrics.rs, tracing.rs"
    - "The hephaestus-api crate compiles with cargo build -p hephaestus-api"
    - "All test stubs pass with cargo test -p hephaestus-api (ignored tests do not fail)"
  artifacts:
    - path: "crates/hephaestus-api/Cargo.toml"
      provides: "Minimal crate manifest for test compilation"
    - path: "crates/hephaestus-api/src/lib.rs"
      provides: "Empty crate root"
    - path: "crates/hephaestus-api/tests/api.rs"
      provides: "Integration test stubs for API-01 and API-02"
    - path: "crates/hephaestus-api/tests/health.rs"
      provides: "Integration test stubs for API-03"
    - path: "crates/hephaestus-api/tests/shutdown.rs"
      provides: "Integration test stubs for API-04"
    - path: "crates/hephaestus-api/tests/metrics.rs"
      provides: "Integration test stubs for OBSV-01"
    - path: "crates/hephaestus-api/tests/tracing.rs"
      provides: "Integration test stubs for OBSV-03"
  key_links:
    - from: "crates/hephaestus-api/tests/*.rs"
      to: "crates/hephaestus-api/src/lib.rs"
      via: "Integration tests compile against the hephaestus-api crate"
---

## Phase Goal

**As a** platform operator, **I want to** deploy Hephaestus as a Kubernetes pod and send HTTP inference requests, **so that** I can serve model inference with health probes, metrics, and tracing in production.

<objective>
Create the hephaestus-api crate skeleton and integration test scaffolding required by the Nyquist validation strategy. This Wave 0 plan establishes the test file structure that 02-01 and 02-02 populate with real test implementations as they build out the HTTP serving layer and observability stack.

Purpose: Satisfy Wave 0 completeness so that every Phase 2 requirement has a corresponding test file before implementation begins. Test stubs are marked #[ignore] and populated by subsequent plans.
Output: Minimal hephaestus-api crate that compiles, plus 5 integration test stub files covering all 8 phase requirements.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-http-serving-and-observability/02-CONTEXT.md
@.planning/phases/02-http-serving-and-observability/02-VALIDATION.md
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hephaestus-api crate skeleton</name>
  <files>crates/hephaestus-api/Cargo.toml, crates/hephaestus-api/src/lib.rs</files>
  <read_first>
    Cargo.toml
  </read_first>
  <action>
    Create the minimal hephaestus-api crate so that integration test files can compile against it. This crate is intentionally empty; 02-01 and 02-02 add the real dependencies and module implementations.

    **Step 1 -- Create crate directory structure**

    Create directory crates/hephaestus-api/src/.

    **Step 2 -- Create crates/hephaestus-api/Cargo.toml**

    Minimal manifest:
    - package name "hephaestus-api", version "0.1.0", edition "2024"
    - No [dependencies] section yet (02-01 adds production deps)
    - [dev-dependencies]: tokio.workspace = true (needed for #[tokio::test] in integration test stubs)

    **Step 3 -- Create crates/hephaestus-api/src/lib.rs**

    Single-line module doc comment describing the HTTP serving layer for Hephaestus. No module declarations or re-exports yet (02-01 adds them).
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith && cargo build -p hephaestus-api 2>&1 | tail -5</automated>
  </verify>
  <done>The hephaestus-api crate exists in the workspace, compiles with cargo build -p hephaestus-api, and is ready for 02-01 to add production dependencies and module implementations.</done>
</task>

<task type="auto">
  <name>Task 2: Create integration test stub files for all phase requirements</name>
  <files>crates/hephaestus-api/tests/api.rs, crates/hephaestus-api/tests/health.rs, crates/hephaestus-api/tests/shutdown.rs, crates/hephaestus-api/tests/metrics.rs, crates/hephaestus-api/tests/tracing.rs</files>
  <action>
    Create integration test stub files with #[ignore] test functions for every Phase 2 requirement. Each stub documents which requirement it covers and which plan will populate it with real test logic.

    **crates/hephaestus-api/tests/api.rs** -- Covers API-01 (POST /infer) and API-02 (GET /healthz/live):
    - Module doc comment describing coverage
    - #[tokio::test] #[ignore = "pending 02-01 implementation"] async fn post_infer_returns_json_classification -- covers API-01
    - #[tokio::test] #[ignore = "pending 02-01 implementation"] async fn get_healthz_live_returns_200_with_metadata -- covers API-02

    **crates/hephaestus-api/tests/health.rs** -- Covers API-03 (readiness probe):
    - #[tokio::test] #[ignore = "pending 02-01 implementation"] async fn readiness_returns_503_before_warmup -- covers API-03
    - #[tokio::test] #[ignore = "pending 02-01 implementation"] async fn readiness_returns_200_after_warmup -- covers API-03

    **crates/hephaestus-api/tests/shutdown.rs** -- Covers API-04 (graceful shutdown):
    - #[tokio::test] #[ignore = "pending 02-01 implementation"] async fn sigterm_flips_readiness_to_503 -- covers API-04
    - #[tokio::test] #[ignore = "pending 02-01 implementation"] async fn inflight_requests_drain_before_exit -- covers API-04

    **crates/hephaestus-api/tests/metrics.rs** -- Covers OBSV-01 (Prometheus metrics) and CORE-04 (request timeout):
    - #[tokio::test] #[ignore = "pending 02-02 implementation"] async fn metrics_endpoint_returns_prometheus_text -- covers OBSV-01
    - #[tokio::test] #[ignore = "pending 02-02 implementation"] async fn stage_duration_histograms_recorded_after_inference -- covers OBSV-01
    - #[tokio::test] #[ignore = "pending 02-01 implementation"] async fn request_timeout_returns_504 -- covers CORE-04

    **crates/hephaestus-api/tests/tracing.rs** -- Covers OBSV-03 (OpenTelemetry tracing) and OBSV-02 (structured logging):
    - #[tokio::test] #[ignore = "pending 02-02 implementation"] async fn telemetry_init_without_otel_does_not_panic -- covers OBSV-03
    - #[tokio::test] #[ignore = "pending 02-02 implementation"] async fn structured_logs_contain_model_id -- covers OBSV-02

    Each test function body is empty (the #[ignore] attribute skips execution). Comments inside each body describe the expected behavior to guide the implementor.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith && cargo test -p hephaestus-api 2>&1 | tail -10</automated>
  </verify>
  <done>Five integration test files exist under crates/hephaestus-api/tests/ covering all 8 phase requirements. All stubs compile and pass (as ignored). cargo test -p hephaestus-api shows the ignored test count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Wave 0 creates only test scaffolding; no runtime trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-W0 | (none) | Test scaffolding only | low | accept | No production code, no network exposure, no user input processing. Test stubs are compile-time artifacts only. |
</threat_model>

<verification>
- cargo build -p hephaestus-api exits 0
- cargo test -p hephaestus-api exits 0 (all tests ignored, none fail)
- cargo test -p hephaestus-api 2>&1 shows 11 ignored tests
- Integration test files exist: tests/api.rs, tests/health.rs, tests/shutdown.rs, tests/metrics.rs, tests/tracing.rs
</verification>

<success_criteria>
The hephaestus-api crate skeleton compiles and all 5 integration test files exist with #[ignore] stubs covering every Phase 2 requirement. Subsequent plans (02-01, 02-02) can fill in test implementations and remove the #[ignore] markers as features are built.
</success_criteria>

<output>
Create `.planning/phases/02-http-serving-and-observability/02-00-SUMMARY.md` when done
</output>

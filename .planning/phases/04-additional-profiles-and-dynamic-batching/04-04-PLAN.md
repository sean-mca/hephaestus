---
phase: 04-additional-profiles-and-dynamic-batching
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - crates/hephaestus/src/config.rs
  - crates/hephaestus/src/main.rs
  - crates/hephaestus-core/src/postprocess.rs
  - crates/hephaestus-core/src/pipeline.rs
autonomous: true
gap_closure: true
requirements: [PROF-04, BTCH-03]
must_haves:
  truths:
    - "Config::validate() rejects batch_max_size outside [1, 64] when batch_enabled is true"
    - "extract_id2label returns CoreError::ModelValidation on non-contiguous keys"
    - "merge_subword_entities computes correct running average across N merged tokens"
    - "batch classifier path returns CoreError::Inference on out-of-range label index (not empty string)"
    - "softmax and argmax_with_score return Result::Err on empty input"
    - "seq2seq decode rejects negative token IDs via checked conversion"
    - "all outputs[0] accesses are guarded against models with zero output tensors"
  artifacts:
    - crates/hephaestus/src/config.rs
    - crates/hephaestus/src/main.rs
    - crates/hephaestus-core/src/postprocess.rs
    - crates/hephaestus-core/src/pipeline.rs
  key_links:
    - "main.rs calls config.validate() immediately after Config::from_env()"
    - "softmax callers propagate the new Result return with the ? operator"
    - "argmax_with_score callers propagate the new Result return with the ? operator"
---

<objective>
Close verification gaps CR-01, CR-02, CR-03 and code review warnings CR-04, WR-01, WR-02, WR-05 identified in 04-VERIFICATION.md and 04-REVIEW.md. These are correctness and robustness bugs in the Phase 04 implementation that were flagged but never fixed.

Purpose: The phase cannot be marked verified with two blockers (config crash on invalid batch_max_size, silent label corruption on non-contiguous id2label) and several warnings (wrong NER scores, inconsistent error handling, panic paths on malformed input). All fixes are mechanical and well-scoped.

**Deferred:** WR-04 (install_recorder() returns anyhow::Error from library crate metrics.rs, violating err-thiserror-lib.md) — out of scope for this gap-closure pass; the metrics crate boundary is unrelated to the verification blockers and can be addressed in a future cleanup phase.

Output: Patched config.rs, main.rs, postprocess.rs, pipeline.rs with tests proving each fix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-additional-profiles-and-dynamic-batching/04-VERIFICATION.md
@.planning/phases/04-additional-profiles-and-dynamic-batching/04-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config validation and postprocess robustness (CR-01, CR-03, WR-01)</name>
  <files>crates/hephaestus/src/config.rs, crates/hephaestus/src/main.rs, crates/hephaestus-core/src/postprocess.rs</files>
  <read_first>
    - crates/hephaestus/src/config.rs (current Config struct, test helpers, existing tests)
    - crates/hephaestus/src/main.rs (startup sequence, where to insert validate call)
    - crates/hephaestus-core/src/postprocess.rs (softmax, argmax_with_score, merge_subword_entities signatures and all callers)
    - crates/hephaestus-core/src/error.rs (CoreError variants for new error returns)
    - rules/err-result-over-panic.md (project rule requiring Result over panic)
  </read_first>
  <behavior>
    - Test: Config with batch_enabled=true and batch_max_size=0 fails validate() with message containing "between 1 and 64"
    - Test: Config with batch_enabled=true and batch_max_size=65 fails validate() with message containing "between 1 and 64"
    - Test: Config with batch_enabled=true and batch_max_size=32 passes validate()
    - Test: Config with batch_enabled=false and batch_max_size=0 passes validate() (validation skipped when batching disabled)
    - Test: Config with batch_enabled=true and batch_max_wait_ms exceeding request_timeout_secs * 1000 fails validate()
    - Test: softmax on empty slice returns Err (not panic)
    - Test: argmax_with_score on empty slice returns Err (not panic)
    - Test: merge_subword_entities with 3 merged tokens produces score equal to arithmetic mean of all 3 scores (not pairwise cascade)
  </behavior>
  <action>
    **CR-01 — Config::validate():**
    Add a pub fn validate(&self) method to Config in config.rs. When batch_enabled is true, reject batch_max_size outside the range [1, 64] inclusive using anyhow::bail with a message naming the valid range and the actual value. Also validate that batch_max_wait_ms is less than request_timeout_secs * 1000 (per IN-02 from the review). When batch_enabled is false, skip batch validation entirely. In main.rs, call config.validate()? on the line immediately after Config::from_env()? (before telemetry init at line 30). Remove the underscore prefix from _receiver on line 119-120 of main.rs (WR-03 from review).

    **WR-01 — softmax and argmax_with_score return Result:**
    Change softmax signature from pub(crate) fn softmax(logits: &[f32]) -> Vec<f32> to pub(crate) fn softmax(logits: &[f32]) -> Result<Vec<f32>, CoreError>. Add an early check: if logits.is_empty(), return Err(CoreError::Inference("empty logits slice".into())). Wrap the existing computation in Ok(). Update the doc comment to replace the Panics section with an Errors section. Requires adding use crate::error::CoreError at the top of postprocess.rs (or adjusting the existing import path).

    Change argmax_with_score signature from pub(crate) fn argmax_with_score(probs: &[f32]) -> (usize, f32) to pub(crate) fn argmax_with_score(probs: &[f32]) -> Result<(usize, f32), CoreError>. Add an early check: if probs.is_empty(), return Err(CoreError::Inference("empty probability slice".into())). Wrap the existing fold in Ok(). Update the doc comment to replace the Panics section with an Errors section.

    Update all existing tests in postprocess.rs that call softmax or argmax_with_score to unwrap the Result (append .expect("...") or use ? in the test). Add two new tests: test_softmax_empty_returns_error and test_argmax_empty_returns_error that assert the Err variant is returned.

    **CR-03 — Running average in merge_subword_entities:**
    In the merge_subword_entities function, replace the pairwise averaging on line 239 (prev.score = (prev.score + score) / 2.0) with a running sum approach. Add a parallel Vec of u32 counts initialized alongside the entities Vec. When extending an entity (should_extend branch), increment the count and add the new score to prev.score (which accumulates the sum during the loop). After the entity-building loop completes, divide each entity's accumulated score by its count to produce the true arithmetic mean. This ensures that for N merged tokens with scores s1..sN, the final score is (s1+s2+...+sN)/N regardless of merge order.

    Add a test test_merge_running_average that constructs a mock tokenizer Encoding (via tokenizers::Encoding::new) with 3 subword tokens belonging to the same word, all labeled I-PER, with scores 0.9, 0.8, 0.7. Assert the merged entity score equals (0.9 + 0.8 + 0.7) / 3.0 within f32 epsilon, not the pairwise result of 0.7625.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith && cargo test -p hephaestus --lib -- config::tests && cargo test -p hephaestus-core --lib -- postprocess::tests</automated>
  </verify>
  <acceptance_criteria>
    - config.rs contains pub fn validate(&self) -> Result<(), anyhow::Error>
    - main.rs line after Config::from_env()? calls config.validate()?
    - main.rs no longer has _receiver variable name (renamed to receiver)
    - softmax signature returns Result<Vec<f32>, CoreError>
    - argmax_with_score signature returns Result<(usize, f32), CoreError>
    - test_softmax_empty_returns_error exists and passes
    - test_argmax_empty_returns_error exists and passes
    - merge_subword_entities no longer contains the expression (prev.score + score) / 2.0
    - test_merge_running_average exists and asserts score == (0.9 + 0.8 + 0.7) / 3.0 within epsilon
    - cargo test -p hephaestus --lib passes
    - cargo test -p hephaestus-core --lib -- postprocess::tests passes
  </acceptance_criteria>
  <done>Config::validate() rejects invalid batch config before any resource allocation. softmax and argmax_with_score return Result on empty input per err-result-over-panic.md. Entity score merging produces correct arithmetic mean across all merged tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline correctness and defensive output access (CR-02, CR-04, WR-02, WR-05)</name>
  <files>crates/hephaestus-core/src/pipeline.rs</files>
  <read_first>
    - crates/hephaestus-core/src/pipeline.rs (full file: extract_id2label, batch_postprocess_classifier, Seq2SeqPipeline::execute, batch_postprocess_seq2seq, all outputs[0] sites, all callers of softmax/argmax_with_score)
    - crates/hephaestus-core/src/postprocess.rs (to see the new Result signatures from Task 1)
    - crates/hephaestus-core/src/error.rs (CoreError variants)
  </read_first>
  <action>
    **CR-02 — Validate contiguous id2label keys:**
    In extract_id2label (around line 1033), after sorting entries by numeric key, add a validation loop: for each (expected_index, (actual_key, _label)) in entries.iter().enumerate(), if expected_index != actual_key, return Err(CoreError::ModelValidation(format!("id2label keys must be contiguous from 0; expected key {expected_index}, found {actual_key}"))). This catches gaps like {0: "A", 2: "C"} before the keys are discarded.

    Add a test test_id2label_rejects_noncontiguous that creates a config.json Value with id2label {0: "NEG", 2: "POS"} (gap at 1), calls extract_id2label, and asserts the result is Err with a message containing "contiguous".

    **CR-04 — Batch classifier error on out-of-range label:**
    In batch_postprocess_classifier (around line 843), replace id2label.get(idx).cloned().unwrap_or_default() with a match: Some(l) returns l.clone(), None returns Err(CoreError::Inference(format!("argmax index {idx} out of range for id2label (len {})", id2label.len()))). This matches the single-request error path at lines 309-318.

    **WR-01 ripple — Update softmax and argmax_with_score callers:**
    After Task 1, softmax and argmax_with_score return Result. Update every call site in pipeline.rs:
    - ClassifierPipeline::execute (line 303): postprocess::softmax(logits_data) becomes postprocess::softmax(logits_data)?
    - ClassifierPipeline::execute (line 306): postprocess::argmax_with_score(&probs) becomes postprocess::argmax_with_score(&probs)?
    - batch_postprocess_classifier (line 841): postprocess::softmax(sample_logits) becomes postprocess::softmax(sample_logits)? (the closure already returns Result, so ? works)
    - batch_postprocess_classifier (line 842): postprocess::argmax_with_score(&probs) becomes postprocess::argmax_with_score(&probs)?
    - argmax_per_token in postprocess.rs calls argmax_with_score internally -- that call also needs updating to propagate the Result (check postprocess.rs; if Task 1 already handled this, skip)

    **WR-02 — Checked integer casts in seq2seq decode:**
    In Seq2SeqPipeline::execute (lines 446, 449) and batch_postprocess_seq2seq (lines 904, 923), replace bare id as u32 casts with u32::try_from(id).map_err(|_| CoreError::Inference(format!("negative token ID {id} in seq2seq output")))?. For the f32 path, replace v.round() as u32 with a check: if v.round() < 0.0 or v.round() > u32::MAX as f32, return Err; otherwise cast. In the single-request execute, the closure uses .map().collect() so convert to a fallible iterator using .map(...).collect::<Result<Vec<u32>, CoreError>>()?. In batch postprocess, apply the same pattern per sample within the existing closure that already returns Result.

    **WR-05 — Guard all outputs[0] accesses:**
    There are 9 sites where outputs[0] is directly indexed (lines 297, 371, 444, 549, 821, 856, 894, 913, 946). For the 4 sites in single-request execute methods (297, 371, 444, 549), introduce a helper function fn first_output(outputs: &ort::session::SessionOutputs) -> Result<&ort::Value, CoreError> that checks if the outputs collection is non-empty (try outputs.iter().next() or check a len method) and returns Err(CoreError::Inference("model produced no output tensors".into())) if empty. Use this helper to replace direct outputs[0] access. For the 5 sites in batch postprocess functions (821, 856, 894, 913, 946), apply the same checked access pattern. If SessionOutputs does not expose .len() or .get(), use a match on outputs.iter().next() to get the first entry, then extract the tensor from it. Verify the ort v2 API surface by checking the ort crate source or docs before choosing the access pattern.

    Add test test_id2label_accepts_contiguous to confirm {0: "A", 1: "B", 2: "C"} still works after the contiguity check.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith && cargo test -p hephaestus-core --lib && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - extract_id2label with input {0: "NEG", 2: "POS"} returns Err containing "contiguous"
    - extract_id2label with input {0: "NEG", 1: "POS"} returns Ok
    - batch_postprocess_classifier does not contain unwrap_or_default() for label lookup
    - No bare `id as u32` casts remain in Seq2SeqPipeline::execute or batch_postprocess_seq2seq
    - No bare `v.round() as u32` casts remain in those same functions
    - No bare outputs[0] indexing remains in pipeline.rs (all guarded by a checked access pattern)
    - All softmax/argmax_with_score call sites in pipeline.rs use ? to propagate Result
    - cargo test -p hephaestus-core --lib passes
    - cargo clippy --workspace -- -D warnings passes
  </acceptance_criteria>
  <done>Pipeline correctness bugs fixed: non-contiguous id2label is rejected, batch classifier errors properly on out-of-range index, seq2seq casts are checked, and all output tensor access is guarded against empty outputs.</done>
</task>

<task type="auto">
  <name>Task 3: Full workspace validation</name>
  <files>crates/hephaestus-core/src/pipeline.rs, crates/hephaestus-core/src/postprocess.rs, crates/hephaestus/src/config.rs, crates/hephaestus/src/main.rs</files>
  <read_first>
    - crates/hephaestus/src/config.rs (verify validate() exists)
    - crates/hephaestus/src/main.rs (verify validate() call and receiver rename)
    - crates/hephaestus-core/src/postprocess.rs (verify Result returns and running average)
    - crates/hephaestus-core/src/pipeline.rs (verify all fixes applied)
  </read_first>
  <action>
    Run the full workspace test suite and clippy. Fix any compilation errors from the signature changes rippling across crate boundaries. If argmax_per_token in postprocess.rs calls argmax_with_score and was not updated in Task 1, update it now to propagate the Result (change its return type to Result<Vec<(usize, f32)>, CoreError> and update callers in pipeline.rs). Verify that cargo build --workspace, cargo test --workspace, and cargo clippy --workspace -- -D warnings all pass cleanly. Fix the clippy let_unit_value warning in batcher.rs test code (line 188) if it surfaces with --all-targets by removing the let binding or using a plain expression.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith && cargo build --workspace && cargo test --workspace && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - cargo build --workspace exits 0
    - cargo test --workspace exits 0 with all tests passing
    - cargo clippy --workspace -- -D warnings exits 0
    - No remaining uses of the pairwise average pattern in postprocess.rs
    - No remaining bare outputs[0] in pipeline.rs
    - No remaining unchecked casts in seq2seq paths
    - Config::validate() is called in main.rs before any resource allocation
  </acceptance_criteria>
  <done>Entire workspace compiles, all tests pass, clippy is clean. All verification gaps from 04-VERIFICATION.md and code review warnings from 04-REVIEW.md are resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars -> Config | Operator-supplied configuration enters the runtime |
| model config.json -> id2label | Model-packaged metadata used for inference output mapping |
| ONNX output tensors -> postprocess | Model-produced tensor data drives label/score computation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering | Config::batch_max_size | high | mitigate | Config::validate() rejects values outside [1, 64] before Batcher construction (CR-01 fix) |
| T-04-07 | Information Disclosure | extract_id2label | medium | mitigate | Validate contiguous keys 0..N; reject with clear error instead of silently misindexing labels (CR-02 fix) |
| T-04-08 | Denial of Service | softmax/argmax on empty input | medium | mitigate | Return Result::Err instead of panicking on empty slices (WR-01 fix) |
| T-04-09 | Denial of Service | outputs[0] on zero-output model | medium | mitigate | Checked access with error return instead of panic (WR-05 fix) |
| T-04-10 | Tampering | seq2seq i64 as u32 cast | low | mitigate | Use u32::try_from with error on negative values instead of wrapping cast (WR-02 fix) |
</threat_model>

<verification>
1. cargo build --workspace exits 0
2. cargo test --workspace exits 0 (all existing + new tests pass)
3. cargo clippy --workspace -- -D warnings exits 0
4. Config::validate() test covers batch_max_size=0 (rejected), batch_max_size=65 (rejected), batch_max_size=32 (accepted)
5. extract_id2label test covers non-contiguous keys (rejected) and contiguous keys (accepted)
6. softmax and argmax_with_score tests cover empty input (Err returned)
7. merge_subword_entities test covers 3-token merge with correct arithmetic mean
</verification>

<success_criteria>
All 3 blocker gaps (CR-01, CR-02, CR-03) and 3 code review warnings (CR-04, WR-01, WR-02, WR-05) from 04-VERIFICATION.md and 04-REVIEW.md are resolved with tests proving correctness. The workspace builds, tests pass, and clippy is clean.
</success_criteria>

## Artifacts this phase produces

**New symbols:**
- `Config::validate(&self) -> Result<(), anyhow::Error>` in crates/hephaestus/src/config.rs
- `first_output` helper function (or equivalent checked access pattern) in crates/hephaestus-core/src/pipeline.rs

**Modified signatures:**
- `postprocess::softmax(logits: &[f32]) -> Result<Vec<f32>, CoreError>` (was bare Vec)
- `postprocess::argmax_with_score(probs: &[f32]) -> Result<(usize, f32), CoreError>` (was bare tuple)
- `postprocess::argmax_per_token` may change to return `Result<Vec<(usize, f32)>, CoreError>` if it calls argmax_with_score internally

**Modified behavior:**
- `extract_id2label` rejects non-contiguous id2label keys with CoreError::ModelValidation
- `merge_subword_entities` uses running sum/count for score averaging
- `batch_postprocess_classifier` returns CoreError::Inference on out-of-range label index
- Seq2seq decode paths use checked u32 conversion
- All `outputs[0]` sites use checked access

**New tests:**
- test_validate_rejects_zero_batch_size
- test_validate_rejects_large_batch_size
- test_validate_accepts_valid_batch_size
- test_validate_skips_when_batching_disabled
- test_validate_rejects_wait_exceeding_timeout
- test_softmax_empty_returns_error
- test_argmax_empty_returns_error
- test_merge_running_average
- test_id2label_rejects_noncontiguous
- test_id2label_accepts_contiguous

<output>
Create `.planning/phases/04-additional-profiles-and-dynamic-batching/04-04-SUMMARY.md` when done
</output>

---
phase: 10-grpc-inference-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/hephaestus-proto/Cargo.toml
  - crates/hephaestus-proto/build.rs
  - crates/hephaestus-proto/src/lib.rs
  - crates/hephaestus-proto/proto/hephaestus/v1/inference.proto
autonomous: true
requirements:
  - SC-05
must_haves:
  truths:
    - "Proto messages compile to Rust types via tonic-prost-build codegen"
    - "FILE_DESCRIPTOR_SET is available as a public constant for reflection"
    - "InferRequest and InferResponse are constructable and serializable"
    - "InferResponse carries result as opaque JSON bytes — no per-profile proto types"
  artifacts:
    - crates/hephaestus-proto/proto/hephaestus/v1/inference.proto
    - crates/hephaestus-proto/build.rs
    - crates/hephaestus-proto/src/lib.rs
    - crates/hephaestus-proto/Cargo.toml
  key_links:
    - "build.rs compile_protos path matches proto file location"
    - "include_proto! macro argument matches proto package name (hephaestus.v1)"
    - "file_descriptor_set_path filename matches include_file_descriptor_set! argument"
---

<objective>
Create the hephaestus-proto crate with protobuf definitions, tonic-prost-build codegen, and public generated types for the gRPC inference API.

Purpose: Establish the gRPC message and service contract that Wave 2 (Plan 10-02) will implement. The proto crate is a standalone build dependency with no downstream coupling, making it a clean first wave.

Output: A compilable hephaestus-proto crate exporting v1 module with InferenceService trait, request/response types, and FILE_DESCRIPTOR_SET for reflection.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-grpc-inference-api/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proto file, build.rs codegen, and workspace dependencies</name>
  <files>
    Cargo.toml,
    crates/hephaestus-proto/Cargo.toml,
    crates/hephaestus-proto/build.rs,
    crates/hephaestus-proto/proto/hephaestus/v1/inference.proto
  </files>
  <action>
Add workspace-level dependencies to the root Cargo.toml [workspace.dependencies] section: tonic = "0.14", tonic-health = "0.14", tonic-reflection = "0.14", prost = "0.14", prost-types = "0.14".

Rewrite crates/hephaestus-proto/Cargo.toml to declare dependencies on tonic and prost (both workspace = true), and a [build-dependencies] section with tonic-prost-build = "0.14". The crate edition must be 2024.

Create directory crates/hephaestus-proto/proto/hephaestus/v1/ and write inference.proto with:
- syntax proto3, package hephaestus.v1
- service InferenceService with a single unary rpc Infer(InferRequest) returns (InferResponse)
- message InferRequest with string text = 1
- message InferResponse with string model_id = 1, uint64 latency_ms = 2, bytes result_json = 3
  (result_json carries the model-determined JSON output as opaque bytes — same payload the REST API returns, no per-profile proto types needed)

Create build.rs that uses tonic_prost_build::configure() to set file_descriptor_set_path to OUT_DIR/hephaestus_descriptor.bin, then calls compile_protos with the proto file path and proto root include directory. The function signature is fn main() -> Result<(), Box<dyn std::error::Error>>.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/hephaestus && cargo check -p hephaestus-proto 2>&1 | tail -5</automated>
  </verify>
  <done>Proto file compiles via tonic-prost-build. cargo check succeeds for hephaestus-proto. Workspace Cargo.toml has tonic/prost workspace deps.</done>
</task>

<task type="auto">
  <name>Task 2: lib.rs with generated types and unit tests</name>
  <files>
    crates/hephaestus-proto/src/lib.rs
  </files>
  <action>
Rewrite crates/hephaestus-proto/src/lib.rs to:

1. Add a module-level doc comment describing the crate as protobuf definitions and generated gRPC types for the Hephaestus inference API.

2. Declare a pub mod v1 block that calls tonic::include_proto!("hephaestus.v1") to include the generated code.

3. Declare a public constant FILE_DESCRIPTOR_SET of type &[u8] using tonic::include_file_descriptor_set!("hephaestus_descriptor") for gRPC server reflection.

4. Add a #[cfg(test)] mod tests block with these tests:
   - infer_request_roundtrip: construct an InferRequest with text "hello", encode it with prost::Message::encode, decode it back, assert text matches.
   - infer_response_roundtrip: construct an InferResponse with model_id, latency_ms, and result_json containing a JSON bytes payload. Encode, decode, assert all fields match and result_json deserializes to expected JSON.
   - file_descriptor_set_is_nonempty: assert FILE_DESCRIPTOR_SET.len() > 0 (confirms build.rs generated the descriptor).

Import prost::Message in the test module for encode/decode.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/hephaestus && cargo test -p hephaestus-proto --lib 2>&1 | tail -15</automated>
  </verify>
  <done>All 3 unit tests pass. hephaestus-proto exports v1 module with InferRequest, InferResponse (generic result_json), and FILE_DESCRIPTOR_SET constant. cargo test -p hephaestus-proto succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Proto definition | Defines the gRPC API contract; no runtime trust boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | tonic-prost-build codegen | low | accept | Build-time only dependency; generates deterministic output from proto files. Crate verified in Package Legitimacy Audit. |
| T-10-SC | Tampering | cargo dependency installs | medium | mitigate | All packages verified in RESEARCH.md Package Legitimacy Audit. No ASSUMED or SUS packages. |
</threat_model>

<verification>
cargo test -p hephaestus-proto --lib passes with all tests green.
cargo check -p hephaestus-proto compiles without warnings.
Proto file exists at crates/hephaestus-proto/proto/hephaestus/v1/inference.proto.
FILE_DESCRIPTOR_SET constant is non-empty (validated by test).
</verification>

<success_criteria>
- hephaestus-proto crate compiles with tonic-prost-build codegen
- InferRequest and InferResponse (with generic result_json bytes field) are constructable in Rust
- InferenceService server trait is generated (InferenceServiceServer type exists)
- FILE_DESCRIPTOR_SET is exported for reflection use in Plan 10-02
- 3 unit tests pass validating message roundtrip serialization and descriptor availability
</success_criteria>

<output>
Create `.planning/phases/10-grpc-inference-api/10-01-SUMMARY.md` when done
</output>

---
phase: 06-opendal-storage-abstraction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - forge/src/forge/storage.py
  - forge/src/forge/config.py
  - forge/src/forge/queue.py
  - forge/pyproject.toml
  - forge/tests/conftest.py
  - forge/tests/test_storage.py
autonomous: false
requirements:
  - STOR-01
must_haves:
  truths:
    - "Forge uploads converted models via opendal.Operator instead of boto3 (D-06)"
    - "Forge uses STORAGE_* env vars matching Hephaestus config (D-07)"
    - "opendal replaces boto3 as a required dependency (D-10)"
    - "Forge validation logic is unchanged (D-09)"
    - "asyncio.to_thread wrapping preserved for synchronous opendal.Operator calls"
    - "Tests use opendal memory backend instead of moto S3 mock (D-14)"
  artifacts:
    - path: "forge/src/forge/storage.py"
      provides: "OpenDAL-based upload function"
      contains: "opendal.Operator"
    - path: "forge/src/forge/config.py"
      provides: "ForgeSettings with storage_type, storage_bucket, storage_prefix fields"
      contains: "storage_type"
    - path: "forge/pyproject.toml"
      provides: "Updated dependencies with opendal replacing boto3"
      contains: "opendal"
  key_links:
    - from: "forge/src/forge/queue.py"
      to: "forge/src/forge/storage.py"
      via: "import and call upload_to_storage in _do_convert"
      pattern: "upload_to_storage"
    - from: "forge/src/forge/storage.py"
      to: "forge/src/forge/config.py"
      via: "build_operator reads ForgeSettings fields for Operator construction"
      pattern: "build_operator"
---

<objective>
Migrate the Forge Python service from boto3 to OpenDAL Python bindings for model upload operations (D-06, D-07, D-10). Update config to use STORAGE_* env vars, replace the upload function, update the conversion queue call site, and rewrite tests to use OpenDAL memory backend instead of moto S3 mocks.

Purpose: Both Rust and Python services must use the same storage abstraction (D-06). This plan handles the Python side independently from the Rust migration, enabling parallel execution.
Output: Forge service uses opendal for all storage operations, boto3/moto fully removed, tests pass with memory backend.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-opendal-storage-abstraction/06-CONTEXT.md
@.planning/phases/06-opendal-storage-abstraction/06-RESEARCH.md
@.planning/phases/06-opendal-storage-abstraction/06-PATTERNS.md

@forge/src/forge/storage.py
@forge/src/forge/config.py
@forge/src/forge/queue.py
@forge/pyproject.toml
@forge/tests/conftest.py
@forge/tests/test_storage.py
</context>

## Artifacts this phase produces

### Plan 06-03 artifacts

**Modified:** `forge/src/forge/storage.py`
- Removed: `import boto3`, `from boto3.s3.transfer import TransferConfig`, `upload_to_s3` function
- Added: `import opendal`
- Added function: `build_operator(settings) -> opendal.Operator` — constructs Operator from ForgeSettings fields
- Added function: `upload_to_storage(op: opendal.Operator, model_id: str, local_dir: str) -> list[str]` — walks local_dir, writes each file via op.write()

**Modified:** `forge/src/forge/config.py`
- Removed fields: `s3_bucket: str`, `s3_prefix: str`
- Added fields: `storage_type: str = "s3"`, `storage_bucket: str = ""`, `storage_prefix: str = ""`, `storage_root: str = ""`, `storage_region: str = ""`

**Modified:** `forge/src/forge/queue.py`
- Updated import: `from forge.storage import upload_to_storage, build_operator` (was `upload_to_s3`)
- Updated `_do_convert` to build operator and call `upload_to_storage`

**Modified:** `forge/pyproject.toml`
- Added dependency: `opendal>=0.47`
- Removed dependency: `boto3>=1.35`
- Removed dev dependency: `moto[s3]>=5.0`

**Modified:** `forge/tests/conftest.py`
- Removed: `import boto3`, `from moto import mock_aws`, `s3_mock` fixture
- Added: `import opendal`
- Added fixture: `memory_operator() -> opendal.Operator` using `opendal.Operator("memory")`
- Updated `test_settings` fixture with new STORAGE_* fields

**Modified:** `forge/tests/test_storage.py`
- Rewrote all tests to use `upload_to_storage` with memory operator instead of `upload_to_s3` with moto mock

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify Python opendal package legitimacy</name>
  <files>forge/pyproject.toml</files>
  <what-built>Python opendal package legitimacy verification (flagged SUS in Package Legitimacy Audit)</what-built>
  <action>
    Pause for human verification of the Python opendal package before proceeding with installation.
    The package was flagged SUS by automated checks. Human must confirm it is the official Apache
    OpenDAL Python binding before adding it to forge/pyproject.toml.
  </action>
  <how-to-verify>
    The Python opendal package was flagged SUS by automated checks due to latest release recency
    and missing download stats. However, it is the official Apache OpenDAL Python binding from
    the apache/opendal GitHub repository (Apache Software Foundation governance).

    Please verify:
    1. Visit https://pypi.org/project/opendal/ and confirm the package is maintained by the
       Apache Software Foundation (check Author and Home-page fields)
    2. Visit https://github.com/apache/opendal/tree/main/bindings/python and confirm this is
       the official Python binding from the Apache OpenDAL monorepo
    3. Confirm you approve adding opendal to the Forge pyproject.toml as a replacement for boto3
  </how-to-verify>
  <verify>Human confirms the Python opendal package is the official Apache project binding</verify>
  <done>Human approved opendal Python package for installation</done>
  <resume-signal>Type "approved" to proceed with the Forge OpenDAL migration, or describe concerns</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Update Forge storage, config, queue, and pyproject.toml</name>
  <files>forge/src/forge/storage.py, forge/src/forge/config.py, forge/src/forge/queue.py, forge/pyproject.toml</files>
  <read_first>
    forge/src/forge/storage.py
    forge/src/forge/config.py
    forge/src/forge/queue.py
    forge/pyproject.toml
    .planning/phases/06-opendal-storage-abstraction/06-CONTEXT.md
    .planning/phases/06-opendal-storage-abstraction/06-RESEARCH.md
    .planning/phases/06-opendal-storage-abstraction/06-PATTERNS.md
  </read_first>
  <action>
    In `forge/pyproject.toml`:
    - Replace `"boto3>=1.35",` with `"opendal>=0.47",` in `dependencies` list per D-10
    - Remove `"moto[s3]>=5.0",` from `[dependency-groups] dev` list per D-10

    In `forge/src/forge/config.py`:
    - Replace `s3_bucket: str = ""` with `storage_type: str = "s3"` per D-02, D-07
    - Replace `s3_prefix: str = ""` with `storage_bucket: str = ""` per D-07
    - Add fields after storage_bucket: `storage_prefix: str = ""`, `storage_root: str = ""`, `storage_region: str = ""` per D-07
    - Update the class docstring to reference `STORAGE_TYPE`, `STORAGE_BUCKET`, etc. instead of `S3_BUCKET`

    In `forge/src/forge/storage.py`:
    - Replace the module docstring to describe OpenDAL-based model storage operations
    - Remove `import boto3` and `from boto3.s3.transfer import TransferConfig`
    - Add `import opendal`
    - Keep `import os` (still needed for os.walk, os.path operations)

    Add function `build_operator(settings) -> opendal.Operator`:
    - Takes a `ForgeSettings` instance
    - Constructs kwargs dict from settings fields:
      - If settings.storage_bucket: add `bucket=settings.storage_bucket`
      - If settings.storage_region: add `region=settings.storage_region`
      - If settings.storage_type == "fs" and settings.storage_root: add `root=settings.storage_root`
        - If settings.storage_prefix: combine as `root=f"{settings.storage_root}/{settings.storage_prefix}"`
      - If settings.storage_type != "fs" and settings.storage_prefix: add `root=f"/{settings.storage_prefix}"`
    - Return `opendal.Operator(settings.storage_type, **kwargs)`

    Replace `upload_to_s3` function with `upload_to_storage(op: opendal.Operator, model_id: str, local_dir: str) -> list[str]`:
    - Same recursive walk logic using os.walk(local_dir) with sorted(files)
    - For each file: compute relative path, build storage path as `f"{model_id}/{relative}"`
      (no prefix parameter needed — Operator root already includes it per D-04)
    - Read file bytes with `open(filepath, "rb").read()`
    - Write via `op.write(path, data)` (synchronous opendal.Operator, per Pitfall 4 from RESEARCH.md)
    - Append path to uploaded list
    - Return list of uploaded paths

    In `forge/src/forge/queue.py`:
    - Replace `from forge.storage import upload_to_s3` with `from forge.storage import upload_to_storage, build_operator`
    - In `_do_convert` method (lines 76-107):
      - Before the upload call, construct the operator: `op = build_operator(settings)`
      - Replace the `asyncio.to_thread(upload_to_s3, output_dir, settings.s3_bucket, settings.s3_prefix, model_id)` call with `asyncio.to_thread(upload_to_storage, op, model_id, output_dir)`
      - The `asyncio.to_thread` wrapping is preserved because `opendal.Operator` is synchronous per Pitfall 4
    - The rest of queue.py (semaphore, locks, caching, cleanup) remains unchanged per D-08
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith/forge && python -c "from forge.storage import upload_to_storage, build_operator; from forge.config import ForgeSettings; print('imports ok')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `forge/pyproject.toml` contains `"opendal>=0.47"` and does NOT contain `"boto3"` or `"moto"`
    - `forge/src/forge/storage.py` contains `import opendal` and does NOT contain `import boto3`
    - `forge/src/forge/storage.py` contains `def upload_to_storage` and `def build_operator`
    - `forge/src/forge/storage.py` does NOT contain `def upload_to_s3`
    - `forge/src/forge/config.py` contains `storage_type: str = "s3"` and does NOT contain `s3_bucket` or `s3_prefix`
    - `forge/src/forge/queue.py` imports `upload_to_storage` and `build_operator` from `forge.storage`
    - `forge/src/forge/queue.py` does NOT contain `upload_to_s3` or `settings.s3_bucket`
    - Python imports succeed without errors
  </acceptance_criteria>
  <done>Forge storage, config, queue updated to use OpenDAL. boto3/moto removed from dependencies. All imports resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Update Forge test fixtures and storage tests for OpenDAL memory backend</name>
  <files>forge/tests/conftest.py, forge/tests/test_storage.py</files>
  <read_first>
    forge/tests/conftest.py
    forge/tests/test_storage.py
    forge/src/forge/storage.py
    forge/src/forge/config.py
  </read_first>
  <action>
    In `forge/tests/conftest.py`:
    - Remove `import boto3` and `from moto import mock_aws`
    - Add `import opendal`
    - Remove the `s3_mock` fixture entirely (lines 42-47)
    - Add a new fixture `memory_operator`:
      ```
      @pytest.fixture()
      def memory_operator() -> opendal.Operator:
          return opendal.Operator("memory")
      ```
    - Update `test_settings` fixture:
      - Replace `s3_bucket=TEST_BUCKET` with `storage_type="memory"` (tests use memory backend)
      - Replace `s3_prefix=TEST_PREFIX` with `storage_bucket=""`, and add `storage_prefix=TEST_PREFIX`
      - Add `storage_root=""`, `storage_region=""`
    - Keep `TEST_BUCKET` and `TEST_PREFIX` constants (TEST_PREFIX is still used in test assertions for path verification)
    - Remove `TEST_BUCKET` if no longer referenced (check test_storage.py after update)

    In `forge/tests/test_storage.py`:
    - Replace `from forge.storage import upload_to_s3` with `from forge.storage import upload_to_storage`
    - Update all test function signatures to use `memory_operator` fixture instead of `s3_mock`

    Rewrite `test_upload_to_s3_with_prefix` as `test_upload_to_storage_writes_files`:
    - Create memory_operator
    - Call `upload_to_storage(memory_operator, "org/my-model", populated_output_dir)`
    - Assert returned list has 3 entries
    - Assert each path follows `"org/my-model/{filename}"` format (no prefix in path since Operator root handles it)
    - Verify files are readable: `memory_operator.read(path)` returns bytes for each path

    Rewrite `test_upload_to_s3_without_prefix` as `test_upload_paths_contain_model_id`:
    - Call upload_to_storage with model_id "org/my-model"
    - Assert all paths start with "org/my-model/"
    - Assert no path starts with "/"

    Rewrite `test_uploaded_files_are_retrievable` as `test_uploaded_files_are_readable`:
    - Upload files via upload_to_storage
    - For each returned path, call `memory_operator.read(path)` and assert `len(data) > 0`

    Rewrite `test_upload_includes_subdirectory_files` as `test_upload_includes_subdirectories`:
    - Same setup (create onnx/ subdir with file)
    - Call upload_to_storage
    - Assert 4 paths returned (3 original + 1 in onnx/)
    - Assert `"org/my-model/onnx/model.onnx"` is in the returned paths

    Run the full Forge test suite to verify everything passes, including test_api.py and test_converter.py (which should be unaffected since they do not import boto3 or storage directly — verify this).
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith/forge && uv run pytest tests/test_storage.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `forge/tests/conftest.py` does NOT contain `import boto3`, `from moto import mock_aws`, or `def s3_mock`
    - `forge/tests/conftest.py` contains `import opendal` and `def memory_operator`
    - `forge/tests/conftest.py` test_settings fixture uses `storage_type` instead of `s3_bucket`
    - `forge/tests/test_storage.py` does NOT contain `upload_to_s3` or `s3_mock`
    - `forge/tests/test_storage.py` uses `upload_to_storage` and `memory_operator` fixture
    - All tests in `forge/tests/test_storage.py` pass
    - `uv run pytest tests/ -v` passes the full Forge test suite (storage + api + converter)
  </acceptance_criteria>
  <done>Forge tests fully migrated to OpenDAL memory backend. No boto3/moto references remain. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars -> ForgeSettings | STORAGE_* env vars configure the Operator; set via k8s manifest |
| local files -> Operator.write | Converted model files uploaded from temp directory to storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Tampering | model_id in storage paths | medium | mitigate | Forge validates model_id via Pydantic field_validator (existing, unchanged). Same validation as Rust validate_model_id. |
| T-06-09 | Information Disclosure | opendal credentials | medium | mitigate | OpenDAL loads credentials from env/IMDS/IRSA. No credentials in ForgeSettings fields or code. |
| T-06-10 | Denial of Service | large file upload | low | accept | Forge already has conversion_timeout_secs and semaphore(1) limiting concurrent work. File sizes bounded by model architecture. Internal service. |
| T-06-SC | Tampering | pip install (opendal) | high | mitigate | opendal Python package flagged SUS in Package Legitimacy Audit. Blocking human-verify checkpoint added before install. Official Apache project binding verified. |
</threat_model>

<verification>
- `cd forge && uv run pytest tests/ -v` — full Forge test suite passes
- `grep -r "boto3\|moto" forge/src/ forge/tests/` returns no matches
- `grep -r "s3_bucket\|s3_prefix" forge/src/forge/config.py` returns no matches
- `grep -r "upload_to_s3" forge/` returns no matches
</verification>

<success_criteria>
Forge service fully migrated from boto3 to OpenDAL Python bindings. Config uses STORAGE_* env vars matching Hephaestus. Tests pass with memory backend. No boto3/moto references remain in Forge source or tests.
</success_criteria>

<output>
Create `.planning/phases/06-opendal-storage-abstraction/06-03-SUMMARY.md` when done
</output>

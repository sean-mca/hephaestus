---
phase: 07-production-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/hephaestus-core/src/pipeline.rs
  - crates/hephaestus-api/src/routes.rs
  - crates/hephaestus/src/main.rs
  - crates/hephaestus-resolve/src/resolver.rs
autonomous: true
requirements: []
---

<objective>
Fix five production-readiness issues identified during code review: add token_type_ids support to unlock BERT-family models, add request body size limits, make warmup failures non-fatal, fix shutdown watchdog to not skip destructors, and make HF download retry skip non-transient errors.
</objective>

<context>
@crates/hephaestus-core/src/pipeline.rs
@crates/hephaestus-api/src/routes.rs
@crates/hephaestus/src/main.rs
@crates/hephaestus-resolve/src/resolver.rs
@crates/hephaestus-resolve/src/error.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add token_type_ids support to run_onnx_inference</name>
  <files>crates/hephaestus-core/src/pipeline.rs</files>
  <read_first>
    crates/hephaestus-core/src/pipeline.rs
  </read_first>
  <action>
    In `run_onnx_inference()` (around line 224):
    - After building input_ids and attention_mask tensors, check if the session expects `token_type_ids` by inspecting `session.inputs()` for an entry named `"token_type_ids"`
    - If present, build a zeros tensor of shape `(1, seq_len)` (same shape as input_ids) using `Array2::zeros((1, seq_len))`
    - Add it to the `ort::inputs!` macro call alongside input_ids and attention_mask
    - If not present, use the existing two-input call (backward compatible with DistilBERT)
    - Use a conditional: build inputs with or without token_type_ids based on the session check

    Also update `pad_and_stack()` to return a third tensor (token_type_ids as zeros) and update `execute_batch()` match arms to pass it through to `session.run()` when the session expects it. The token_type_ids batch tensor is always zeros of shape `(batch_size, max_seq_len)`.

    Add a test that verifies zeros tensor construction matches expected shape.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith && cargo test -p hephaestus-core 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `run_onnx_inference` conditionally includes token_type_ids when the session expects it
    - `execute_batch` also handles token_type_ids for batched inference
    - All existing tests still pass (backward compatible with DistilBERT models)
    - `cargo check --workspace` succeeds
  </acceptance_criteria>
  <done>token_type_ids support added, BERT-family models unlocked</done>
</task>

<task type="auto">
  <name>Task 2: Add request body size limit</name>
  <files>crates/hephaestus-api/src/routes.rs, crates/hephaestus-api/Cargo.toml</files>
  <read_first>
    crates/hephaestus-api/src/routes.rs
    crates/hephaestus-api/Cargo.toml
  </read_first>
  <action>
    In `routes.rs`, add `RequestBodyLimitLayer::new(1024 * 1024)` (1MB limit) to the router via `.layer()`.
    Import `tower_http::limit::RequestBodyLimitLayer`.
    In `crates/hephaestus-api/Cargo.toml`, add the `"limit"` feature to the existing `tower-http` dependency if not already present.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith && cargo check -p hephaestus-api 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Router includes RequestBodyLimitLayer with 1MB limit
    - `cargo check --workspace` succeeds
  </acceptance_criteria>
  <done>Request body size limit added to prevent oversized payloads</done>
</task>

<task type="auto">
  <name>Task 3: Make warmup failure non-fatal, fix shutdown watchdog</name>
  <files>crates/hephaestus/src/main.rs</files>
  <read_first>
    crates/hephaestus/src/main.rs
  </read_first>
  <action>
    Warmup fix: Find the warmup inference section in main.rs. Replace the `?` error propagation with a `match` that logs a warning on failure and continues startup. The pod should still become ready even if warmup fails — warmup is a performance optimization (pre-warms caches), not a correctness gate.

    Shutdown fix: Find the shutdown watchdog that calls `std::process::exit(1)`. Replace with a `CancellationToken` (from `tokio_util::sync`) or a `tokio::sync::Notify`. Share the token between the watchdog task and the graceful shutdown future. The watchdog cancels the token after the drain timeout; `axum::serve().with_graceful_shutdown()` returns normally; OTel shutdown and destructors run cleanly. Add `tokio-util` to `crates/hephaestus/Cargo.toml` if using CancellationToken, or use a bare `Notify` from tokio (already a dep).
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith && cargo check -p hephaestus 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Warmup failure logs a warning but does not crash the pod
    - Shutdown watchdog does not call `process::exit` — uses cancellation instead
    - `cargo check --workspace` succeeds
  </acceptance_criteria>
  <done>Warmup made resilient, shutdown watchdog uses clean cancellation</done>
</task>

<task type="auto">
  <name>Task 4: Make HF retry skip non-transient errors</name>
  <files>crates/hephaestus-resolve/src/resolver.rs, crates/hephaestus-resolve/src/error.rs</files>
  <read_first>
    crates/hephaestus-resolve/src/resolver.rs
    crates/hephaestus-resolve/src/error.rs
  </read_first>
  <action>
    Add an `is_transient(&self) -> bool` method to `ResolveError`:
    - `HuggingFace(msg)` where msg contains "Authentication", "401", "403", "404" → false (not transient)
    - `NoOnnxExport` → false
    - `Storage(msg)` where msg contains "not found" → false
    - Everything else → true (network errors, timeouts, server errors)

    Update `with_retry` in resolver.rs to check `error.is_transient()` before retrying. If not transient, return the error immediately without logging a retry warning.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/minerva/blacksmith && cargo test -p hephaestus-resolve 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ResolveError` has `is_transient()` method
    - `with_retry` breaks early on non-transient errors
    - Auth failures and 404s are not retried
    - Network errors and timeouts are still retried
    - All existing resolver tests pass
  </acceptance_criteria>
  <done>HF retry only retries transient errors</done>
</task>

</tasks>

<verification>
- `cargo build --workspace` — full workspace compiles
- `cargo test --workspace` — all tests pass
- BERT-family models (with token_type_ids) can load and serve inference
</verification>

<success_criteria>
All five production-readiness issues fixed. BERT-family models work. Request body limits enforced. Warmup and shutdown are resilient. Retry logic is smarter.
</success_criteria>

<output>
Create `.planning/phases/07-production-hardening/07-01-SUMMARY.md` when done
</output>
